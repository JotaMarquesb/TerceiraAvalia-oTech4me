unit uContaBancaria;

interface

type
  TContaBancaria = class
  private
    FNumeroConta: string;
    FTitular: string;
    FSaldo: Double;
  public
    constructor Create(const ANumeroConta, ATitular: string);

    procedure Depositar(AValor: Double);
    procedure Sacar(AValor: Double);

    function ExibirSaldo: Double;

    property NumeroConta: string read FNumeroConta;
    property Titular: string read FTitular;
    property Saldo: Double read FSaldo;
  end;

implementation

constructor TContaBancaria.Create(const ANumeroConta, ATitular: string);
begin
  FNumeroConta := ANumeroConta;
  FTitular := ATitular;
  FSaldo := 0.00;
end;

procedure TContaBancaria.Depositar(AValor: Double);
begin
  FSaldo := FSaldo + AValor;
end;

procedure TContaBancaria.Sacar(AValor: Double);
begin
  if AValor > FSaldo then
    raise Exception.Create('Saldo insuficiente.');

  FSaldo := FSaldo - AValor;
end;

function TContaBancaria.ExibirSaldo: Double;
begin
  Result := FSaldo;
end;

end.
